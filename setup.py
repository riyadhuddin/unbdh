from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='we will be working on youth track',
    author='iac',
    license='MIT',
)
